-- Konfigurasi tambahan untuk AI-based cheat detection
aiAnticheatConfig = {
    maxTeleportDistance = 50, -- meter dalam 2 detik
    maxSpeed = 50.0,          -- m/s jika ingin tambahkan speed detection
    maxJumpHeight = 5.0,      -- meter (untuk mendeteksi superjump)
}
